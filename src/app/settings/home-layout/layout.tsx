import type { Metadata } from 'next';
export const metadata: Metadata = { title: '홈 레이아웃 설정 — 글리움' };
export default function HomeLayoutSettingsLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
