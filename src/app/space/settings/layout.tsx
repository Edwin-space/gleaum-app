import type { Metadata } from 'next';
export const metadata: Metadata = { title: '공간 설정 — 글리움' };
export default function SpaceSettingsLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
