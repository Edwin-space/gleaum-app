import type { Metadata } from 'next';
export const metadata: Metadata = { title: '공간 일정 추가 — 글리움' };
export default function SpaceScheduleNewLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
