import type { Metadata } from 'next';
export const metadata: Metadata = { title: '공간 만들기 — 글리움' };
export default function SpaceNewLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
