import type { Metadata } from 'next';
export const metadata: Metadata = { title: '일정 추가 — 글리움' };
export default function NewScheduleLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
